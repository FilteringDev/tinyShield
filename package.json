{
  "name": "@filteringdev/tinyshield",
  "version": "0.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "build:libs": "pnpm --filter @filteringdev/tinyshield-lib run build",
    "build:userscript": "pnpm run build:libs && pnpm --filter @filteringdev/tinyshield-builder run build --minify true --build-type production --SubscriptionUrl https://cdn.jsdelivr.net/npm/@filteringdev/tinyshield@latest/dist/tinyShield.user.js",
    "build": "pnpm run build:userscript",
    "debug": "pnpm --filter @filteringdev/tinyshield-builder run debug",
    "lint": "pnpm --filter @filteringdev/tinyshield-lib run lint && pnpm --filter @filteringdev/tinyshield-builder run lint && pnpm --filter @filteringdev/tinyshield-userscript run lint",
    "test": "pnpm --filter @filteringdev/tinyshield-lib run test"
  },
  "keywords": [
    "Ad-Shield"
  ],
  "files": [
    "dist/**/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FilteringDev/tinyShield.git"
  },
  "license": "MPL-2.0",
  "workspaces": [
    "libs",
    "userscript",
    "builder"
  ],
  "devDependencies": {
    "oxlint": "^1.80.0",
    "oxlint-tsgolint": "^7.0.2001"
  }
}
